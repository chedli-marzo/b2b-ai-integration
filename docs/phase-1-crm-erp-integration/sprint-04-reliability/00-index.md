# Sprint 04: Reliability

**Branch:** `sprint/p1-s04-reliability`, from `main`. Each story gets its own branch from this one, merged back by pull request.

Goal, stories and acceptance criteria: not written yet. Run `/story sprint-04`.

Carried over from us-01: the nightly reconciliation job that catches events missed during outages longer than HubSpot's 24-hour retry window.
